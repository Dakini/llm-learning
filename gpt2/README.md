# GPT2

This folder focuses on the work by Andrej Karparthy and the videos for Makemore, and GPT2 Training.

This was a learning experience to train a transformer model and get into the nitty gritty of model training. The years, I have spent more time with APIs and wisht to be a bit more involved in the training and the use of LLM's

This contains the code from those videos as a learning example, and visualising the models outputs and weights. and visualising the self attention layers.

I am not sharing the log or the model weights due to the size of the model weights.
